// Shared media fallbacks for THE TALK

// Shown whenever Sanity has no image for an episode, short or product.
// The brand card is local, so a fallback never depends on a third-party host.
export const PLACEHOLDER_IMAGE = '/og-image.png';
