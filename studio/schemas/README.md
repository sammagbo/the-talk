# Schémas historiques

Ce dossier contient les schémas JavaScript de l’ancienne version du Studio. Ils sont conservés pendant la migration pour faciliter l’audit des données existantes.

Ils ne sont pas chargés par `sanity.config.ts`. Les schémas actifs et typés se trouvent dans `../schemaTypes/`.
